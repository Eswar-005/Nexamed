# Leave empty for local development (uses Vite proxy /api)
# For production (Vercel deployment connecting to Render backend), set to your backend URL:
VITE_API_BASE_URL=https://nexamed-backend.onrender.com
